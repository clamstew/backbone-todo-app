(function() {
  // console.log("todo list main is running");
  // var todoItem = new TodoItem({id: 3});
  // todoItem.fetch();
  // var todoView = new TodoView({ model: todoItem });
  // todoView.render();
  // console.log(todoView.el);

  // $('#todo-list').html(todoView.el);

  // todoList.forEach(function(todoItem){
  //   console.log("I'm running the todolist foreach loop");
  //   var todoView = new TodoView({ model: todoItem });
  //   todoView.render();
  //   $('#todo-list').append(todoView.el);
  // });

  // todoItem.on('change', function() {
  //   todoView.render();
  //   $('#todo-list').html(todoView.el);
  // });

// var todos = [
//   {description: 'Pick up milk.', status: 'incomplete'},
//   {description: 'Get a car wash', status: 'incomplete'},
//   {description: 'Learn Backbone', status: 'incomplete'}
// ];

// todoList.reset(todos);

  // todoListView.render();
  // console.log("todoListView.el - ", todoListView.el);

  // add new todo item to collection
  // var newTodoItem = new TodoItem({
  //   description: 'Take out trash.',
  //   status: 'incomplete'
  // });
  // todoList.add(newTodoItem);

})();